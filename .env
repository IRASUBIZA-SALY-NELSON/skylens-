# Environment variables for Vite
# Note: In Vite, only variables prefixed with VITE_ are exposed to the client

# Generate a secure key using the generateEncryptionKey() function
VITE_ENCRYPTION_KEY=your-secure-encryption-key-32-characters-long

# Other environment variables can go here
# VITE_API_BASE_URL=http://localhost:3000/api
